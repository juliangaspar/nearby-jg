  <div class="top-right-text">
  <%= render "shared/arrow-svg"%>
    <h3><strong>UP</strong> to <strong>LEND</strong></h4>
  </div>
  <div class="bottom-left-text">
    <%= render "shared/arrow-svg"%>
    <h3><strong>DOWN</strong> to <strong>BORROW</strong></h3>
  </div>

<div data-controller="drag-home"
     data-action="scroll@window->drag-home#nav"
     data-user-path="<%= user_signed_in? ? user_path(current_user) : new_user_session_path %>" data-items-path="<%= items_path %>">
  <div class="main">
    <h1>NEARBY</h1>


    <div class="white-circle">
    </div>

    <div class="yellow-circle">
    </div>

    <div class="anchor" data-drag-home-target="dragging">
    </div>

    <div class="yellow-rect">
    </div>

    <div class="white-rect">
    </div>

  </div>
</div>
